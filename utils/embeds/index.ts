import challenge from './challenge';
import demotion = require('./demotion');
import game = require('./game');
import gameDraw = require('./gameDraw');
import gameLoss = require('./gameLoss');
import gameWin = require('./gameWin');
import leaderboard = require('./leaderboard');
import leave = require('./leave');
import promotion = require('./promotion');
import queueEmbed = require('./queue');
import results = require('./results');
import serverRules = require('./serverRules');
import statsEmbed = require('./stats');
import status = require('./status');
import waiting = require('./waiting');

module.exports = {
    challenge,
    demotion,
    game,
    gameDraw,
    gameLoss,
    gameWin,
    leaderboard,
    leave,
    promotion,
    queueEmbed,
    results,
    serverRules,
    statsEmbed,
    status,
    waiting
};